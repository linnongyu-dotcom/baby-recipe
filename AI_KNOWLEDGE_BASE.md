# AI Knowledge Base

## 持久化餐单与生成规则必须同步升级

周餐单由 Zustand `persist` 写入 `localStorage`。生成器或最终校验器的规则即使已经修正，
也不会自动重算用户浏览器里已有的 `weeklyPlan`；因此用户仍可能看到旧版本产生的不合规组合，
误以为新规则没有生效。

凡是会改变已生成餐单合法性的规则升级，都必须同时：

1. 提升 `PERSIST_VERSION`；
2. 让迁移只清空 `weeklyPlan`，保留宝宝档案、过敏/忌口/喜好、收藏和自定义食谱；
3. 更新迁移专项测试，明确覆盖上一个线上版本到新版本的失效边界；
4. 用生成、整餐刷新和单菜替换测试验证所有写回入口，而不能只测试初次生成。

不要仅依赖页面重新加载或部署新静态资源：二者都不会删除 `localStorage` 中的旧餐单。
