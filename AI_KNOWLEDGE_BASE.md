# AI Knowledge Base

## 食谱角色与名称不能相互替代

- 推荐器按 `dishType` 建立主食、蛋白质、蔬菜候选池，`category` 和菜名只用于展示，不能修正错误的角色数据。
- 新增或修改食谱时，应核对 `category` 与 `dishType` 是否一致。复合主食即使含蔬菜或肉类，仍应标记为 `staple`；否则可能被当作肉菜加入已有主食的餐次，形成双主食或食材重复。
- 餐单多样性必须比较 `mainIngredients` 中的真实食材，不能只比较食谱 ID、菜名或主要 `proteinSource`。不同做法可能仍然是同一种蔬菜或蛋白质。
- 所有周营养补齐、数量补齐等后处理都可能重新引入重复食材，因此全天多样性检查应放在最后一次补菜之后，并再次验证餐次适配、主食数量和必选营养角色。

## 推荐规则更新必须处理持久化餐单

- `weeklyPlan` 由 Zustand `persist` 保存到 `baby-recipe-storage`。修改生成器只影响新生成的计划，浏览器刷新不会自动更新已经保存的周计划。
- 推荐规则或食谱分类发生会影响现有餐单的变更时，必须提升 store 的持久化版本，并在 `migrate` 中将 `weeklyPlan` 设为 `null`；食谱页挂载后会按现有宝宝档案自动生成新版计划。

## 家庭三餐的蛋白质分配

- 1 岁以上午餐作为全天重点餐，使用纯主食搭配一份明确的肉鱼蛋豆菜，不能因为复合主食里有少量肉末就省略主要蛋白质。
- 默认食谱池有合规候选时，午餐明确安排一份红肉；如果用户的过敏或忌口过滤掉全部红肉，才回退禽肉、豆制品等。晚餐优先鱼、虾、禽肉、蛋和豆制品，已有轻蛋白时不再叠加红肉。
- 一餐只能出现一道含蛋菜。判断依据是全部 `mainIngredients`，不能只看菜名或 `dishType`，否则“虾仁滑蛋 + 蒸蛋”等组合会漏检。
- “优先”不能通过先排序再随机洗牌实现。选择时应先筛出目标蛋白质类型的候选池，再在同类型池内随机，以同时保证餐次方向和菜品变化。
- 粥、汤面、馄饨等本身含较多汤水的主食不能再搭配独立汤品，该限制适用于早餐、午餐和晚餐，并应在所有补菜后处理完成后做最终检查。
- 红肉不仅要按餐次分配，还要检查具体来源：早餐已经吃牛肉时，午餐优先换猪肉，避免一天连续安排牛肉；低龄食谱池没有合规替代红肉时才允许回退。
- 午餐的鱼丸汤不能代替独立肉菜。标准家庭午餐至少应有“主食 + 一份独立红肉菜 + 蔬菜”；有汤时汤是可选补充，不是主要蛋白质的替代品。
- “刷新单餐”必须把完整合规食谱池传给家庭餐生成器，通过 `usedIds` 做优先去重而不是预先永久删除。否则到后半周红肉候选会被删空，刷新后的午餐会退化成蛋汤加素菜。
- “换一道”与“刷新整餐”是两条不同状态路径。单菜替换完成后必须用餐次上下文重新校验；若造成午餐无独立红肉、晚餐无轻蛋白、粥配汤或食材重复，应重新生成整餐，不能直接写入持久化状态。
- 整餐生成本身包含随机选择，也必须在返回前通过同一上下文校验并有限重试；不能假设“重新生成一次”必然合规，否则状态层发现单菜替换不合规后，仍可能保存另一份随机产生的不合规餐单。

## 测试必须与分层标签契约一致

- `deriveNutritionTags` 的契约是“营养特点最多 3 个 + 制作特点最多 2 个”，总数最多 5 个。测试不能继续用旧的“所有标签合计最多 3 个”断言，否则会把合法的制作特点误报成回归。
- 随机推荐测试不能只跑一次。整餐刷新和后处理应连续多轮验证，并且所有诊断脚本也必须能够实际执行；失效的导入或过期年龄枚举会让测试表面存在、实际从未覆盖逻辑。
